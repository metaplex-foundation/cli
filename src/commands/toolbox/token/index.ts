import { Command } from '@oclif/core'

export default class ToolboxToken extends Command {

  static override description = 'Token related tools such as create, mint, etc.'

  public async run(): Promise<void> {

  }
}